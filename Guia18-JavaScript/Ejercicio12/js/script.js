let datoIngresado = alert("Ingresar a la consola y hacer la consulta desde ahi :) ");

let obtenerDato = valor => typeof valor;

let tipoDato = obtenerDato(datoIngresado);

// Escribir una función flecha de JavaScript que reciba un argumento y retorne el tipo de
// dato.